## 1.接口测试核心原则：
对数据负责，对结果负责

发送一个请求，获得一个结果 -- 结果是不是预期的

## 2.什么是接口：

Http接口是一个特殊的url地址，当有人访问这个地址后，这个地址会负责给我们 **提供/操作** 数据

### 2.1 接口测试四要素：
- URL地址
- 请求方式
- 请求参数
- 响应结果

## 3.Jmeter

### 3.1 分别测试

### 3.2 测试片段

### 3.3 用户变量定义

### 3.4 关联接口

下一个接口需要上一个的数据 -> json提取器，关联

### 3.5 断言
核心数据：需要断言 参与了业务流程的数据（增删改查），或者页面展示的数据

### 3.6 数据库

JDBC Request：Variable names自定义变量实际是一个列表，所以自定义的变量名实际调用是ids_1，ids_2这样的

需要加载一个jar包

### 3.7 加密解密

https-相对安全--请求发出后【安全通道】--ssl加密

缺点：只能保证网络传输安全 -- 网络代理工具[fiddler,charles] -- 抓https明文


**所以需要更高的安全防御机制，加密接口（通过算法），在请求发送前，将所有参数变为密文。**

银行行业，支付行业，政府项目

1. 加密算法是什么

123明文数据 -> 加密算法 -> sfasdfsa

> 不是要把加密算法写出来
> 
> 了解算法是什么，学会使用工具，进行解密

- 对称加密

[AES,DES]

**加密数据和解密数据用的是同一个密钥**

- 非对称加密

2. 工作中碰到加密接口怎么处理



