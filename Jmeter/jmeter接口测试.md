## 1. 分模块测试
测试计划-线程组-HTTP请求

1. 使用**测试片段**，对于用例的描述，模块化的封装（导出后可以再次导入，导入使用include控制器）
2. 创建线程组，添加逻辑控制器（模块控制器等）
3. 添加监听器

## 2. 公共数据维护

使用配置元件-用户定义的变量，用来存储公共数据。

使用方法：${变量名}

## 3. 多组数据测试（DDT）

外层：逻辑控制器-循环控制器（循环次数设置）
- 配置元件-CSV Data Set Config（csv文件读取，注意数据应当是测试数据+预期结果），设置文件编码，变量名称
- HTTP请求

## 4. 测试结果判定

使用断言-json断言等
